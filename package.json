{
  "name": "photovault-ai",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "api:dev": "VAULT_DIR=$(pwd)/.vault pnpm --filter api dev"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}